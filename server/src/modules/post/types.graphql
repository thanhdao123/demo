extend type Query {
  posts: [Post]
}

extend type Mutation {
  addPost(author: String, comment: String): Post
}

extend type Subscription {
  postAdded: Post
}

type Post {
  author: String
  comment: String
}
