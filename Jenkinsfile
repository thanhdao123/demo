pipeline {
    agent any

    stages {
        stage('Example Build') {
            steps {
                echo "GIT_COMMIT is ${GIT_COMMIT} okok"
            }
        }
        stage('Example Test') {
            steps {
                echo 'Hello, JDK okokoko'
            }
        }
    }
}