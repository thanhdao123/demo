pipeline {
    agent any
    stages {
        stage('Example Build') {
            steps {
                echo "GIT_COMMIT is okok"
            }
        }
        stage('Example Test') {
            steps {
                echo 'Hello, JDK'
            }
        }
    }
}